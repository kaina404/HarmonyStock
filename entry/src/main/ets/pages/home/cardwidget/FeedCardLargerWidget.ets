/**
 * 首页信息流卡片组件
 * 使用这个卡片的时候，
 * 宽度 -> 屏幕宽度的45%
 *
 * 高度 -> 屏幕高度的40%
 *
 */
@Component
export struct FeedCardLargerWidget {
  build() {
    Row() {

    }
    .width('100%')
    .height('100%')
  }
}